/**
 * Where the donation flow is rendered:
 * - `footer`: a discreet chrome button that opens the flow in a modal.
 * - `inline`: an expanded block embedded at the end of a chapter.
 */
export type DonationDisplayMode = "footer" | "inline";
