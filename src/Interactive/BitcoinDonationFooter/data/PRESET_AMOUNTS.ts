/**
 * Preset donation amounts in EUR. The 21 is a silent bitcoiner easter egg
 * (21M max supply) — it intentionally carries no "coffee" sub-label (spec §4.1).
 */
export const PRESET_AMOUNTS: number[] = [1, 5, 10, 21];
