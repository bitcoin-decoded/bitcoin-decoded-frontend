/**
 * Capital tier (Q1). Internal notation only - never shown to the reader.
 * P1 = a few hundred €, P2 = a few thousand €, P3 = hundreds of thousands € +.
 */
export type PathTier = "P1" | "P2" | "P3";
