/**
 * Approximate year the block subsidy reaches zero (last satoshis mined). Used as
 * the "End" quick-jump on the dial. Distinct from TIME_MACHINE_MAX_YEAR, which
 * is just how far past the end the slider lets you wander.
 */
export const TIME_MACHINE_END_YEAR = 2140;
